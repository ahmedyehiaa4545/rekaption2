{
  "name": "rekaption-frontend",
  "version": "1.0.0",
  "description": "Frontend for ReKaption",
  "scripts": {
    "start": "serve . -l ${PORT:-8080}"
  },
  "dependencies": {
    "serve": "^14.2.4"
  }
}
